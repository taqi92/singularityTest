import 'package:flutter/material.dart';

class CustomStatusCip extends StatelessWidget {
  const CustomStatusCip({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
